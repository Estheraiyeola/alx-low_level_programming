#include"main.h"

/**
 * print_last_digit - prints the last digit of a number
 * @n: number
 * Return: the last digit of the number or 0
 */
int print_last_digit(int n)
{
	int print_last_digit = n % 10;
	return (print_last_digit);
}
